<cu-custom bgColor="bg-gradual-blue" isBack="{{true}}">
	<view slot="backText">返回</view>
	<view slot="content">订单</view>
</cu-custom>
	<form>
	<view class="cu-form-group margin-top">
		<view class="title">求助者</view>
		<text class="text-cut" bindtap="showModal" data-target="Human1">{{elements[idx].human1}}</text>
	</view>
	<view class="cu-modal {{modalName=='Human1'?'show':''}}">
  	<view class="cu-dialog">
    	<view class="cu-bar bg-white shadow">
      	<text>{{elements[idx].human1}}</text>
      	<view class="action" bindtap="hideModal">
        	<text class="cuIcon-close text-red"></text>
      	</view>
    	</view>
  	</view>
	</view>
	<view class="cu-form-group">
		<view class="title">帮助者</view>
		<text class="text-cut" bindtap="showModal" data-target="Human2">{{elements[idx].human2}}</text>
	</view>
	<view class="cu-modal {{modalName=='Human2'?'show':''}}">
  	<view class="cu-dialog">
    	<view class="cu-bar bg-white shadow">
      	<text>{{elements[idx].human2}}</text>
      	<view class="action" bindtap="hideModal">
        	<text class="cuIcon-close text-red"></text>
      	</view>
    	</view>
  	</view>
	</view>
	<view class="cu-form-group">
		<view class="title">起始地址</view>
		<text class="text-cut" bindtap="showModal" data-target="Start">{{elements[idx].start}}</text>
		<text class="cuIcon-locationfill text-blue"></text>
	</view>
	<view class="cu-modal {{modalName=='Start'?'show':''}}">
  	<view class="cu-dialog">
    	<view class="cu-bar bg-white shadow">
      	<text>{{elements[idx].start}}</text>
      	<view class="action" bindtap="hideModal">
        	<text class="cuIcon-close text-blue"></text>
      	</view>
    	</view>
  	</view>
	</view>
	<view class="cu-form-group">
		<view class="title">目的地址</view>
		<text class="text-cut" bindtap="showModal" data-target="Target">{{elements[idx].target}}</text>
		<text class="cuIcon-locationfill text-blue"></text>
	</view>
	<view class="cu-modal {{modalName=='Target'?'show':''}}">
  	<view class="cu-dialog">
    	<view class="cu-bar bg-white shadow">
      	<text>{{elements[idx].target}}</text>
      	<view class="action" bindtap="hideModal">
        	<text class="cuIcon-close text-red"></text>
      	</view>
    	</view>
  	</view>
	</view>
	<view class="cu-form-group">
		<view class="title">保护信息</view>
		<text class="text-cut" bindtap="showModal" data-target="Self">{{elements[idx].self}}</text>
	</view>
	<view class="cu-modal {{modalName=='Self'?'show':''}}">
  	<view class="cu-dialog">
    	<view class="cu-bar bg-white shadow">
      	<text>{{elements[idx].self}}</text>
      	<view class="action" bindtap="hideModal">
        	<text class="cuIcon-close text-red"></text>
      	</view>
    	</view>
  	</view>
	</view>
	<view class="cu-form-group">
		<view class="title">订单描述</view>
		<text class="text-cut" bindtap="showModal" data-target="Message">{{elements[idx].message}}</text>
	</view>
	<view class="cu-modal {{modalName=='Message'?'show':''}}">
  	<view class="cu-dialog">
    	<view class="cu-bar bg-white shadow">
      	<text>{{elements[idx].message}}</text>
      	<view class="action" bindtap="hideModal">
        	<text class="cuIcon-close text-red"></text>
      	</view>
    	</view>
  	</view>
	</view>
	<view class="cu-form-group">
		<view class="title">完成时间</view>
		<text class="text-cut" bindtap="showModal" >{{elements[idx].time}}</text>
	</view>
	<view class="cu-form-group">
		<view class="title">评价信息</view>
		<text class="text-cut" bindtap="showModal" data-target="State">{{elements[idx].state}}</text>
	</view>
	<view class="cu-modal {{modalName=='State'?'show':''}}">
  	<view class="cu-dialog">
    	<view class="cu-bar bg-white shadow">
      	<text>{{elements[idx].state}}</text>
      	<view class="action" bindtap="hideModal">
        	<text class="cuIcon-close text-red"></text>
      	</view>
    	</view>
  	</view>
	</view>
	<view class="cu-form-group">
		<view class="title">代币数额</view>
		<text class="text-cut" bindtap="showModal">{{elements[idx].money}}</text>
	</view>
  <view class="cu-list menu {{menuBorder?'sm-border':''}} {{menuCard?'card-menu margin-top':''}}">
    <view class="cu-item arrow">
      <navigator class="content" url="/pages/orderCenter/chat/chat" hover-class="none">
        <text class="title">聊天</text>
      </navigator>
		</view>
	</view>
	<view class="padding flex flex-direction">
  	<button class="cu-btn bg-red margin-tb-sm lg">删除订单</button>
	</view>
</form>
